describe("pow", function()
    {
        //対象のユースケースを記載。人間が読める形式で書く。
        it("raises to n-th power", function()
        {
            //テスト関数
            assert.equal(pow(2, 3), 8);
        });
    });